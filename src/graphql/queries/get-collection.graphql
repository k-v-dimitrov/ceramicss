query GetCollection($handle: String!) {
    collection(handle: $handle) {
        ...coreCollection
        products(first: 250) {
            nodes {
                ...coreProduct
            }
        }
    }
}
