query GetProduct($id: ID!) {
    product(id: $id) {
        title
        description
        images(first: 5) {
            edges {
                node {
                    altText
                    height
                    width
                    id
                    url
                }
            }
        }
    }
}
