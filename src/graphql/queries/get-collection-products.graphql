query GetCollectionProducts($id: ID!) {
    collection(id: $id) {
        products(first: 100) {
            edges {
                node {
                    ...coreProduct
                }
            }
        }
    }
}
